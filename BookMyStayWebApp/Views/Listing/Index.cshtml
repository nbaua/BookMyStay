@model List<ListingDTO>

<div class="container-md">
    <div class="row mt-3 mb-3">
        <div class="col">
            <h3><i class="bi bi-list-ul"></i>&nbsp;Popular Listings</h3>
        </div>
        <div class="col text-end">
            <a class="btn btn-info" asp-area="" asp-controller="Listing" asp-action="Manage" asp-route-id="0">
                <i class="bi bi-plus-lg"></i>&nbsp;Add Listing
            </a>
        </div>
    </div>

    <table class="table caption-top table-bordered - table-striped" id="ListingTable">
        <thead>
            <tr>
                <th scope="col" class="col text-center">#</th>
                <th scope="col" class="col">Name</th>
                <th scope="col" class="col-6 text-center">Description</th>
                <th scope="col" class="col text-center">Discount</th>
                <th scope="col" class="col text-center">&nbsp;</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <th scope="col" class="col text-center">@item.ListingId</th>
                    <th scope="col" class="col">@item.Name</th>
                    <th scope="col" class="col-6">@item.Description</th>
                    <th scope="col" class="col text-center">@item.ListingPrice</th>
                    <th scope="col" class="col text-center">
                        <a class="btn btn-outline-success" asp-controller="Listing" asp-action="Manage" asp-route-id="@item.ListingId">
                            <i class="bi bi-pencil"></i>
                        </a>
                        <a class="btn btn-outline-danger" asp-controller="Listing" asp-action="Delete" asp-route-id="@item.ListingId" onclick="deleteConfirmation(event);">
                            <i class="bi bi-trash3"></i>
                        </a>
                    </th>
                </tr>
            }
        </tbody>
    </table>
</div>
